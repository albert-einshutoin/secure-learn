# Release Policy

Secure Learn uses Semantic Versioning. `VERSION`, the Git tag, and the changelog version must match. A release tag is created only from `main` after required checks have passed.

## Release gate

A release is publishable only when all of the following are true:

1. Source, unit, integration, Compose, generated-document, and Suricata rule checks pass.
2. A fresh stack with empty volumes completes SIEM bootstrap and the telemetry-backed S3 E2E check.
3. Trivy reports no fixed HIGH or CRITICAL vulnerabilities in the application and Suricata images. Unfixed findings are intentionally excluded from this blocking report and must be assessed separately when base-image updates are reviewed.
4. Syft produces SPDX JSON SBOM files for both built images.
5. SHA-256 checksums and GitHub artifact attestations are generated for the release evidence.

The release workflow uploads scan reports and SBOMs to the GitHub Release. Source archives are generated by GitHub from the immutable tag. Container images are rebuilt locally from digest-pinned bases; this project does not currently publish a registry image.

## Support and disclosure

Only the newest minor release receives routine fixes. Security reports follow [SECURITY.md](../SECURITY.md). A compromised or materially incorrect release is not overwritten: publish a new patch version and document the impact and migration in `CHANGELOG.md`.
