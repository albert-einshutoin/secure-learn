# Only variables consumed by Docker Compose belong here. The base lab uses
# deterministic local-only defaults so copied examples cannot silently change
# its security topology.

# Optional: used by docker-compose.alerting.yml. Keep the placeholder or set a
# real webhook only in an untracked .env file.
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL
